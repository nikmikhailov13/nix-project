spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/nix?currentSchema=nix
    username: postgres
    password: ${password}
liquibase:
  liquibaseSchemaName:nix